<div class="container">

    <div class="row">
        <div class="col">
            <h1>Boardgame Categories</h1>
        </div>
    </div>

    <div class="row">
        <% @categories.each do |category| %>
            <div class="col-4">
                <%= link_to category["name"], "/categories/#{category["id"]}" %>
            </div>
        <% end %>
    </div>
</div>